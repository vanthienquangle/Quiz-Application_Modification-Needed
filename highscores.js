//const highScoresList = document.getElementById("highScoresList");
//const highScores = JSON.parse(localStorage.getItem("highScores")) || [];

//highScoresList.innerHTML = highScores  // Set the innerHTML of the 'highScoresList' element to a formatted string of high scores.
//  .map(score => {  // Use the map() function to iterate over each 'score' object in the 'highScores' array and turn it into an HTML string.
//    return `<li class="high-score">${score.name} - ${score.score}</li>`; 
//    // For each 'score' object, return an HTML string representing a list item ('<li>') with the player's name and their score.
//    // Example: <li class="high-score">Alice - 100</li>
//  })
//  .join("");  // The map() function returns an array of strings. Use join("") to concatenate all the strings together into one single string without commas between them.
